let collection = [];

function addToCollection( artist, title, year ){
    {
  collection.push( artist, title, year );
  }
  console.log( 'in my collection', collection );
}

addToCollection( 'Pulp', 'His and Hers', '1994');
